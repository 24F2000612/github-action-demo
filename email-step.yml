name: Trigger Action with Email in Step

on:
  workflow_dispatch: # Allows manual triggering

jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: 24f2000612@ds.study.iitm.ac.in # Step with your email
        run: |
          echo "Hello, this is a test step with the email address"
          echo "Email address is: 24f2000612@ds.study.iitm.ac.in"
        
      - name: Print Success Message
        run: |
          echo "Action triggered successfully!"
